//package com.ingcollege.service;
//
//import org.junit.runner.RunWith;
//import org.springframework.boot.test.context.SpringBootTest;
//import org.springframework.test.context.junit4.SpringRunner;
//
//@RunWith(SpringRunner.class)
//@SpringBootTest
//public class CoursesServiceImplTest {
	/*
	 * 
	 * @InjectMocks CoursesServiceImpl coursesServiceImpl;
	 * 
	 * @Mock CoursesRepository coursesRepository;
	 * 
	 * 
	 * 
	 * @Test public void testFindAllCourses(){ List<Courses> courses = new
	 * ArrayList<>(); Courses course = new Courses();
	 * 
	 * course.setCourseId(1L); course.setCourseName("Big Data");
	 * course.setCourseCode("BD1167"); course.setCourseDuration("6 Months");
	 * course.setCourseLevel( "Advance Beginner"); course.setProfessor(
	 * "Dr.Jason Daniel"); course.setCourseFee(0.0);
	 * course.setCourseStartDate("26-06-2019"); courses.add(course);
	 * when(coursesRepository.findAll()).thenReturn(courses); List<CoursesDTO>
	 * coursesDtos = coursesServiceImpl.findAllCourses();
	 * 
	 * Assert.assertEquals(courses.size(), coursesDtos.size());
	 * assertEquals(course.getCourseName(), coursesDtos.get(0).getCourseName());
	 * assertEquals(course.getCourseDuration(),
	 * coursesDtos.get(0).getCourseDuration()); assertEquals(course.getCourseFee(),
	 * coursesDtos.get(0).getCourseFee()); assertEquals(course.getCourseId(),
	 * coursesDtos.get(0).getCourseId()); assertEquals(course.getProfessor(),
	 * coursesDtos.get(0).getProfessor()); assertEquals(course.getCourseLevel(),
	 * coursesDtos.get(0).getCourseLevel());
	 * assertEquals(course.getCourseStartDate(),
	 * coursesDtos.get(0).getCourseStartDate());
	 * assertEquals(course.getCourseCode(), coursesDtos.get(0).getCourseCode()); }
	 * 
	 * 
	 * 
	 */
//}
	
